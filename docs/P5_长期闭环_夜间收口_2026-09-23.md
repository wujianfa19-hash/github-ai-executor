# P5 长期闭环夜间收口（2026-09-23）

状态：长期自动迭代系统尚未验收完成。本文件只记录公开仓库里的代码事实，不是永久大脑正式规则。

## 停在这里

- 六环代码在 `02_程序组件/自我升级引擎/`，候选仍走原有 `evaluateCandidate`。
- 写回开关 `writebackEnabled=false`。没有新定时任务。
- 已接入 2026-09-20 至 2026-09-22 的 8 条公开 Actions 元数据。根因全部未核实。
- 合格候选数是 0。不同日期只有 3 天，少于预先写定的 5 天，影子运行保持待持续观察。
- 测试：`npm run test:self-upgrade`，26 pass / 0 fail。这是单元测试，不是真实用户效果已改善。

## 下次可以自动继续的条件

同时满足才谈启用写回：至少 5 个不同日期的脱敏真实事件、安全项全过、目标问题有改善、基线 SHA 未变、目标路径仍在白名单。缺任何一条就继续影子模式。

## 回滚

丢弃分支 `work/p5-long-loop-shadow-20260923`。稳定点是 `main` 的 `7b0f88513e4472c32ecd21ab75f2b14d3091bac5`。
